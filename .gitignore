# Build artifacts (regenerated by `make`)
/vxv
/internal/initbin/vxv-init

# Note: third_party/libkrun/libkrun.so.1 is intentionally committed — vxv bundles
# this patched libkrun (see third_party/libkrun/build.sh) and ships it.

# Editor / scratch
nvim.log
/testdir/*.txt

# Local, per-user Claude Code settings
.claude/settings.local.json
